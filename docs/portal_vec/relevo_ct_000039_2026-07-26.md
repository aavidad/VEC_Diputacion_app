# Relevo CT 000039 — 26 de julio de 2026

## Rama estable

- rama: `integracion/ct-o4-04e-20260726`;
- cabeza publicada: `656c2b697852cb050745b8d78b937b32a104df76`;
- árbol limpio y sincronizado con GitHub al cerrar la sesión.

Últimos productos cerrados:

- `9655d62`: sesión, perfil y huella estable de filtros en Go;
- `183e563`: revalidación final VEC-AD-3;
- `0dc2edc`: revalidación de identidad corporativa;
- `656c2b6`: revisión y documentación de las fronteras C2-D2.

## Candidato CT 000039 preservado

El trabajo interrumpido se ha guardado sin incorporarlo a la rama estable:

- rama remota: `respaldo/ct-000039-20260726-231556`;
- commit: `02d357e`;
- estado: candidato WIP, no revisado ni autorizado para integración.

Contiene:

- migración ascendente y descendente del registrador de acceso RRHH v2;
- pruebas SQL;
- runner PostgreSQL 18.4.

Comprobaciones realizadas antes del respaldo:

- cuatro ficheros identificados;
- todos por debajo de 800 líneas;
- `bash -n` verde;
- `git diff --check` verde;
- `gitleaks` sin hallazgos.

No se ejecutó todavía la puerta PostgreSQL 18.4 completa ni una revisión
independiente. No se debe promover `02d357e` directamente.

## Reanudación obligatoria

1. Crear una rama de integración desde `656c2b6`.
2. Aplicar `02d357e` sin asumir que es correcto.
3. Revisar contrato, barreras `18/2 → 19/3`, ACL, RLS, inmutabilidad,
   concurrencia, índice as-of y safe-down.
4. Ejecutar el runner PostgreSQL 18.4 fijado por digest.
5. Corregir cualquier hallazgo.
6. Obtener una revisión independiente sobre hashes finales.
7. Solo entonces crear un commit definitivo y actualizar la documentación.

Después de CT `000039` siguen:

- CT `000040`: motor tipado de consulta y cursores;
- CT `000041`: fachadas exteriores nominales;
- adaptador PostgreSQL Go;
- composición raíz, web y E2E.

## Métrica oficial

- Contratación temporal: `19/46`, 41 %;
- O4-05: `3/5`;
- Bolsa: `1/14`, 7 %;
- producción: `NO-GO`.
