# Relevo CT 000039 — 26 de julio de 2026

## Rama estable

- rama: `integracion/ct-o4-04e-20260726`;
- cabeza publicada al cierre final: `c286af19df5cda8d18a8490a0e407bb8eb02447b`;
- árbol limpio y sincronizado con GitHub al cerrar la sesión.

Últimos productos cerrados:

- `9655d62`: sesión, perfil y huella estable de filtros en Go;
- `183e563`: revalidación final VEC-AD-3;
- `0dc2edc`: revalidación de identidad corporativa;
- `656c2b6`: revisión y documentación de las fronteras C2-D2.
- `c286af1`: CT `000039` integrado con registrador v2 minimizado, índice
  *as-of* y reversión segura.

## Candidato CT 000039 preservado

El trabajo interrumpido se ha guardado sin incorporarlo a la rama estable:

- rama remota: `respaldo/ct-000039-20260726-231556`;
- commit: `02d357e`;
- estado: candidato WIP, no revisado ni autorizado para integración.

Contiene:

- migración ascendente y descendente del registrador de acceso RRHH v2;
- pruebas SQL;
- runner PostgreSQL 18.4.

Comprobaciones realizadas antes del respaldo:

- cuatro ficheros identificados;
- todos por debajo de 800 líneas;
- `bash -n` verde;
- `git diff --check` verde;
- `gitleaks` sin hallazgos.

No se ejecutó todavía la puerta PostgreSQL 18.4 completa ni una revisión
independiente. No se debe promover `02d357e` directamente.

## Reanudación obligatoria

1. Crear una rama de integración desde `656c2b6`.
2. Aplicar `02d357e` sin asumir que es correcto.
3. Revisar contrato, barreras `18/2 → 19/3`, ACL, RLS, inmutabilidad,
   concurrencia, índice as-of y safe-down.
4. Ejecutar el runner PostgreSQL 18.4 fijado por digest.
5. Corregir cualquier hallazgo.
6. Obtener una revisión independiente sobre hashes finales.
7. Solo entonces crear un commit definitivo y actualizar la documentación.

Después de CT `000039` siguen:

- CT `000040`: tipos, cánones y controles del motor;
- CT `000041`: contrato probatorio de resultados y recibo V2;
- CT `000042`: lectura y confirmación internas;
- CT `000043`: fachadas exteriores nominales;
- adaptador PostgreSQL Go;
- composición raíz, web y E2E.

## Cierre posterior del candidato

La sección anterior conserva la foto histórica del 26 de julio. No debe
interpretarse como estado vigente. El candidato fue corregido, probado y
promovido el 28 de julio:

- rama de trabajo preservada: `integracion/ct-000039-20260726`, cabeza
  `72978e8`;
- commit estable condensado: `c286af1`;
- PostgreSQL 18.4 reproducido por productor, dirección y dos revisores;
- doble dictamen independiente `GO`;
- Gitleaks, ShellCheck y comprobaciones estáticas verdes;
- cero contenedores, ranuras lógicas o temporales residuales.

La evidencia final está en
[la revisión de CT 000039](revisiones/o4_05_revision_registrador_v2_ct_000039_2026-07-28.md).
No se debe recuperar ni promover el antiguo respaldo `02d357e`.

CT `000040` quedó cerrada posteriormente con contrato tipado, canon de
alcance, igualdad sesión-control y reversión segura. La evidencia vigente está
en [la revisión de CT 000040](revisiones/o4_05_revision_contrato_tipado_ct_000040_2026-07-28.md).
El trabajo continúa en CT `000041`; CT `000039` no debe reabrirse salvo
defecto reproducible.

## Métrica oficial

- Contratación temporal: `20/46`, 43 %;
- O4-05: `3/5`;
- Bolsa: `1/14`, 7 %;
- producción: `NO-GO`.
