import { crearTraductorPersonal, formatearFechaEstructuraOrganizativa, formatearRecuentoEstructura } from "./i18n.js?v=20260920-personal-estructura-v1";
function nodo(documento, etiqueta, texto = "") { const salida = documento.createElement(etiqueta); if (texto) salida.textContent = texto; return salida; }
function sigue(raiz, contenedor) { return raiz.querySelector?.("[data-personal-estructura-organizativa-publica]") === contenedor; }
function tabla(documento, estructura, t) { const tabla = nodo(documento, "table"); tabla.className = "tabla-datos"; tabla.append(nodo(documento, "caption", t("estructura_tabla"))); const thead = nodo(documento, "thead"); const cab = nodo(documento, "tr"); ["estructura_cabecera_clave", "estructura_cabecera_etiqueta", "estructura_cabecera_tipo", "estructura_cabecera_adscripcion"].forEach((clave) => { const celda = nodo(documento, "th", t(clave)); celda.setAttribute("scope", "col"); cab.append(celda); }); thead.append(cab); tabla.append(thead); const cuerpo = nodo(documento, "tbody"); estructura.unidades.forEach((u) => { const fila = nodo(documento, "tr"); const clave = nodo(documento, "th", u.clave); clave.setAttribute("scope", "row"); fila.append(clave, nodo(documento, "td", u.etiqueta), nodo(documento, "td", t(`estructura_tipo_${u.tipo}`)), nodo(documento, "td", u.adscripcion_clave || t("estructura_sin_adscripcion"))); cuerpo.append(fila); }); tabla.append(cuerpo); const caja = nodo(documento, "div"); caja.className = "tabla-contenedor"; caja.setAttribute("tabindex", "0"); caja.setAttribute("role", "region"); caja.setAttribute("aria-label", t("estructura_tabla")); caja.append(tabla); return caja; }
function pintar(raiz, contenedor, estado, t) { if (!sigue(raiz, contenedor)) return; const documento = contenedor.ownerDocument; contenedor.replaceChildren(); const cabecera = nodo(documento, "header"); cabecera.className = "cabecera-vista"; cabecera.append(nodo(documento, "p", t("estructura_sobrelinea")), nodo(documento, "h2", t("estructura_titulo")), nodo(documento, "p", t("estructura_ayuda"))); contenedor.append(cabecera); if (estado.tipo === "cargando") { const carga = nodo(documento, "p", t("estructura_cargando")); carga.setAttribute("role", "status"); carga.setAttribute("aria-live", "polite"); contenedor.append(carga); return; } if (estado.tipo === "error") { const e = nodo(documento, "p", t("estructura_error")); e.setAttribute("role", "alert"); contenedor.append(e); return; } const e = estado.estructura; const huella = nodo(documento, "p", t("estructura_huella", { huella: e.fuente.huella_sha256 })); huella.className = "rpt-huella"; contenedor.append(nodo(documento, "p", t("estructura_aviso")), nodo(documento, "p", t("estructura_fuente", { ...e.fuente, actualizada_en: formatearFechaEstructuraOrganizativa(e.fuente.actualizada_en) })), huella, nodo(documento, "p", formatearRecuentoEstructura(e.unidades.length)), tabla(documento, e, t)); }
export async function montarModuloEstructuraOrganizativaPublica({ raiz, cliente, anunciar = () => {}, registrarDesmontar } = {}) { if (!raiz?.append || !cliente?.obtener || typeof anunciar !== "function" || (registrarDesmontar !== undefined && typeof registrarDesmontar !== "function")) throw new TypeError("módulo estructura organizativa pública no disponible"); const documento = raiz.ownerDocument; if (!documento?.createElement) throw new TypeError("documento estructura organizativa pública no disponible"); const t = crearTraductorPersonal(); const contenedor = nodo(documento, "section"); contenedor.className = "modulo-personal"; contenedor.dataset.personalEstructuraOrganizativaPublica = ""; raiz.append(contenedor); let activa = true; const controlador = new AbortController(); const desmontar = () => { if (!activa) return; activa = false; controlador.abort(); contenedor.remove?.(); }; registrarDesmontar?.(desmontar); pintar(raiz, contenedor, { tipo: "cargando" }, t); try { const estructura = await cliente.obtener({ signal: controlador.signal }); if (activa && sigue(raiz, contenedor) && !controlador.signal.aborted) pintar(raiz, contenedor, { tipo: "disponible", estructura }, t); } catch { if (activa && sigue(raiz, contenedor) && !controlador.signal.aborted) { anunciar(t("estructura_error"), "error"); pintar(raiz, contenedor, { tipo: "error" }, t); } } return Object.freeze({ desmontar }); }
