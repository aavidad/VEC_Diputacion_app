import assert from "node:assert/strict";
import test from "node:test";
import { readFileSync } from "node:fs";
import { montarModuloEstructuraOrganizativaPublica } from "./vista-estructura-organizativa-publica.js";

test("estructura carga el i18n actualizado del corte F2", () => {
  const codigo = readFileSync(new URL("./vista-estructura-organizativa-publica.js", import.meta.url), "utf8");
  assert.match(codigo, /from "\.\/i18n\.js\?v=20260924-f2-web2";/u);
});
function raiz() { class Nodo { constructor(documento, etiqueta = "div") { this.ownerDocument = documento; this.tagName = etiqueta; this.children = []; this.dataset = {}; this.parent = null; this.textContent = ""; this.atributos = new Map(); } append(...nodos) { this.children.push(...nodos); nodos.forEach((n) => { n.parent = this; }); } replaceChildren(...nodos) { this.children = []; this.append(...nodos); } removeChild(n) { this.children = this.children.filter((h) => h !== n); n.parent = null; } remove() { this.parent?.removeChild(this); } setAttribute(k, v) { this.atributos.set(k, v); } matches(s) { const k = s.match(/^\[data-([a-z-]+)\]$/u)?.[1]?.replace(/-([a-z])/g, (_m, l) => l.toUpperCase()); return k ? this.dataset[k] !== undefined : false; } querySelector(s) { if (this.matches(s)) return this; for (const h of this.children) { const e = h.querySelector(s); if (e) return e; } return null; } } const d = { createElement: (e) => new Nodo(d, e) }; return new Nodo(d, "root"); }
function estructura() { const unidades = []; for (let i = 0; i < 14; i += 1) unidades.push({ clave: `delegacion-${i}`, etiqueta: "Delegación", tipo: "delegacion" }); for (let i = 0; i < 41; i += 1) unidades.push({ clave: `centro-${i}`, etiqueta: "Centro", tipo: "centro", adscripcion_clave: "delegacion-0" }); for (let i = 0; i < 11; i += 1) unidades.push({ clave: `puesto-${i}`, etiqueta: "Jefatura", tipo: "puesto_responsabilidad", adscripcion_clave: "centro-0" }); return { unidades, fuente: { revision: "demo-v1", actualizada_en: "2026-09-06T00:00:00Z", demostracion: true, aviso: "Demo sin vigencia", huella_sha256: "0e52d878526d6a5e7ee4ab6f525ef92a70144aef665f0b031fca6051564e054c" } }; }
test("vista estructura muestra carga, límites, 66 filas y tabla desplazable", async () => { const r = raiz(); const modulo = await montarModuloEstructuraOrganizativaPublica({ raiz: r, cliente: { async obtener() { return estructura(); } } }); const c = r.querySelector("[data-personal-estructura-organizativa-publica]"); const texto = c.children.map((n) => n.textContent).join(" "); assert.match(texto, /no acredita vigencia administrativa/); assert.match(texto, /14 delegaciones/); assert.match(texto, /Europe\/Madrid/); assert.doesNotMatch(texto, /2026-09-06T00:00:00Z/); const tabla = c.children.find((n) => n.className === "tabla-contenedor"); assert.equal(tabla.atributos.get("role"), "region"); assert.equal(tabla.atributos.get("tabindex"), "0"); modulo.desmontar(); });
test("vista estructura error y desmontaje abortan sin pintar respuesta tardía", async () => { const r = raiz(); const avisos = []; await montarModuloEstructuraOrganizativaPublica({ raiz: r, anunciar: (...a) => avisos.push(a), cliente: { async obtener() { throw new Error("503"); } } }); assert.equal(avisos.length, 1); let resolver; let signal; let limpiar; const r2 = raiz(); const montaje = montarModuloEstructuraOrganizativaPublica({ raiz: r2, registrarDesmontar: (f) => { limpiar = f; }, cliente: { obtener({ signal: s }) { signal = s; return new Promise((resolve) => { resolver = resolve; }); } } }); await Promise.resolve(); limpiar(); resolver(estructura()); const modulo = await montaje; modulo.desmontar(); assert.equal(signal.aborted, true); assert.equal(r2.querySelector("[data-personal-estructura-organizativa-publica]"), null); });
